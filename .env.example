# Alpaca paper-trading API credentials.
# Sign up at https://alpaca.markets, generate paper-trading keys, paste below,
# then copy this file to `.env` (which is gitignored).
APCA_API_KEY_ID=your_paper_key_id_here
APCA_API_SECRET_KEY=your_paper_secret_here
